const path = require('path');

module.exports = {
  ACTION_RUN: 'icon-triangle-right',
  ACTION_STOP: 'icon-primitive-square',
  CLI_PATH: path.join(__dirname, '../', 'node_modules', '.bin', 'react-native'),
};
